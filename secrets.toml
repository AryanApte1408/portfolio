# Streamlit secrets file
# =========================
# IMPORTANT: Do NOT commit this file to GitHub.
# Add it to .gitignore immediately.
#
# For local testing: replace the password below and save.
# For Streamlit Cloud deployment: do NOT upload this file.
# Instead, paste the same content into the "Secrets" box in your
# Streamlit Cloud app settings (Manage app → Settings → Secrets).

app_password = "portfolio-class"
